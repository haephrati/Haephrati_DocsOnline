# Haephrati_DocsOnline
A Google Docs based backup application

## Unit tests

`Tests/DocsOnlineTests` checks `version.h` (`STR_VER` 1.3.0.0) and dialog/command resource IDs (`IDD_DOCSONLINE_DIALOG`, `ID_STARTSTOP`, `ID_CONVERTDOCS`). Build Release|x64.
